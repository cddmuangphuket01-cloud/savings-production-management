'use client';
import {useState} from 'react';
export default function Login(){const [u,setU]=useState(''),[p,setP]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})});const d=await r.json();if(r.ok)location.href='/';else setError(d.error||'เข้าสู่ระบบไม่สำเร็จ');setLoading(false)}return <main className="min-h-screen grid place-items-center p-5"><div className="card w-full max-w-md p-7"><div className="text-center mb-7"><div className="text-5xl">💰</div><h1 className="text-2xl font-bold mt-3">เงินออมทรัพย์เพื่อการผลิต</h1><p className="text-gray-500 mt-1">เข้าสู่ระบบบริหารจัดการ</p></div>{error&&<div className="bg-red-50 text-red-700 rounded-lg p-3 mb-4">{error}</div>}<form onSubmit={submit}><label className="text-sm">ชื่อผู้ใช้</label><input className="input mt-1 mb-4" value={u} onChange={e=>setU(e.target.value)} autoComplete="username" required/><label className="text-sm">รหัสผ่าน</label><input type="password" className="input mt-1 mb-5" value={p} onChange={e=>setP(e.target.value)} autoComplete="current-password" required/><button disabled={loading} className="btn btn-primary w-full">{loading?'กำลังเข้าสู่ระบบ…':'เข้าสู่ระบบ'}</button></form><a className="block text-center mt-4 text-sm text-[#0f4c81]" href="/setup">ตั้งค่าผู้ดูแลระบบครั้งแรก</a></div></main>}
