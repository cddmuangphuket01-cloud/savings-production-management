'use client';
import {useEffect,useState} from 'react';
const money=(n:number)=>new Intl.NumberFormat('th-TH',{minimumFractionDigits:2}).format(n);
export default function Members(){const [rows,setRows]=useState<any[]>([]);const [form,setForm]=useState({member_no:'',first_name:'',last_name:'',phone:'',address:''});const load=()=>fetch('/api/members').then(r=>r.json()).then(setRows);useEffect(load,[]);async function add(e:React.FormEvent){e.preventDefault();if(!form.member_no||!form.first_name||!form.last_name)return alert('กรุณากรอกเลขสมาชิก ชื่อ และนามสกุล');const r=await fetch('/api/members',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!r.ok)return alert('เพิ่มสมาชิกไม่สำเร็จ');setForm({member_no:'',first_name:'',last_name:'',phone:'',address:''});load()}return <div className="min-h-screen p-5 md:p-8"><div className="max-w-7xl mx-auto"><a href="/" className="text-[#0f4c81]">← Dashboard</a><h1 className="text-2xl font-bold mt-3 mb-6">👥 สมาชิกเงินออม</h1><div className="grid lg:grid-cols-[360px_1fr] gap-6"><form onSubmit={add} className="card p-5 h-fit"><h2 className="font-bold mb-4">เพิ่มสมาชิก</h2>{[['member_no','เลขสมาชิก'],['first_name','ชื่อ'],['last_name','นามสกุล'],['phone','เบอร์โทรศัพท์'],['address','ที่อยู่']].map(([k,l])=><div className="mb-3" key={k}><label className="text-sm block mb-1">{l}</label><input className="input" value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<button className="btn btn-primary w-full">บันทึกสมาชิก</button></form><div className="card p-5 overflow-auto"><table className="table"><thead><tr><th>เลขสมาชิก</th><th>ชื่อ–สกุล</th><th>โทรศัพท์</th><th>ยอดเงินออม</th><th>สถานะ</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.member_no}</td><td>{r.first_name} {r.last_name}</td><td>{r.phone||'-'}</td><td>{money(Number(r.balance))}</td><td>{r.status==='active'?'ใช้งาน':'ระงับ'}</td></tr>)}{!rows.length&&<tr><td colSpan={5} className="text-center text-gray-500 py-8">ยังไม่มีสมาชิก</td></tr>}</tbody></table></div></div></div></div>}
