'use client';
import {useEffect,useState} from 'react';import {Users,WalletCards,ArrowDownToLine,ArrowUpFromLine,LogOut,PlusCircle,FileText} from 'lucide-react';
type Stats={members:number,balance:number,deposits:number,withdrawals:number};const money=(n:number)=>new Intl.NumberFormat('th-TH',{minimumFractionDigits:2}).format(n)+' บาท';
export default function Home(){const [s,setS]=useState<Stats>({members:0,balance:0,deposits:0,withdrawals:0}),[tx,setTx]=useState<any[]>([]);useEffect(()=>{Promise.all([fetch('/api/dashboard').then(r=>r.json()),fetch('/api/transactions').then(r=>r.json())]).then(([a,b])=>{setS(a);setTx(b.slice(0,8))})},[]);async function logout(){await fetch('/api/auth/logout',{method:'POST'});location.href='/login'}return <div className="min-h-screen md:flex"><aside className="sidebar w-full md:w-64 p-5"><div className="text-xl font-bold mb-8">💰 เงินออมทรัพย์</div><a className="navitem active" href="/">📊 Dashboard</a><a className="navitem" href="/members">👥 สมาชิก</a><a className="navitem" href="/transactions">📒 รายการเงิน</a><a className="navitem" href="/reports">📈 รายงาน</a><button onClick={logout} className="navitem w-full text-left mt-8">🚪 ออกจากระบบ</button></aside><main className="flex-1 p-5 md:p-8"><div className="max-w-7xl mx-auto"><div className="flex flex-wrap justify-between items-center gap-3 mb-7"><div><h1 className="text-2xl font-bold">ภาพรวมระบบ</h1><p className="text-gray-500">บริหารจัดการเงินออมทรัพย์เพื่อการผลิต</p></div><div className="flex gap-2"><a className="btn btn-success" href="/transactions">+ ฝาก/ถอนเงิน</a><a className="btn btn-primary" href="/members">+ สมาชิก</a></div></div><div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4"><Stat icon={<Users/>} title="สมาชิก" value={s.members.toLocaleString('th-TH')+' คน'}/><Stat icon={<WalletCards/>} title="เงินออมคงเหลือ" value={money(s.balance)}/><Stat icon={<ArrowDownToLine/>} title="ยอดฝากสะสม" value={money(s.deposits)}/><Stat icon={<ArrowUpFromLine/>} title="ยอดถอนสะสม" value={money(s.withdrawals)}/></div><div className="card mt-6 p-5"><div className="flex justify-between items-center mb-4"><h2 className="font-bold text-lg">รายการล่าสุด</h2><a className="text-[#0f4c81]" href="/transactions">ดูทั้งหมด →</a></div><div className="overflow-auto"><table className="table"><thead><tr><th>วันที่</th><th>รายการ</th><th>สมาชิก</th><th>ประเภท</th><th>จำนวน</th></tr></thead><tbody>{tx.map(r=><tr key={r.id}><td>{new Date(r.transaction_date).toLocaleDateString('th-TH')}</td><td>{r.transaction_no}</td><td>{r.first_name} {r.last_name}</td><td>{r.transaction_type==='deposit'?'ฝาก':r.transaction_type==='withdrawal'?'ถอน':r.transaction_type==='interest'?'ดอกเบี้ย':'ปรับปรุง'}</td><td>{money(Number(r.amount))}</td></tr>)}{!tx.length&&<tr><td colSpan={5} className="text-center text-gray-500 py-8">ยังไม่มีรายการ</td></tr>}</tbody></table></div></div><div className="grid md:grid-cols-3 gap-4 mt-6"><Quick href="/members" icon={<Users/>} title="จัดการสมาชิก"/><Quick href="/transactions" icon={<PlusCircle/>} title="บันทึกฝาก–ถอน"/><Quick href="/reports" icon={<FileText/>} title="ออกรายงาน"/></div></div></main></div>}
function Stat({icon,title,value}:{icon:React.ReactNode,title:string,value:string}){return <div className="card p-5"><div className="flex justify-between"><span className="text-gray-500">{title}</span><span className="text-[#0f4c81]">{icon}</span></div><div className="text-xl font-bold mt-4">{value}</div></div>};function Quick({href,icon,title}:{href:string,icon:React.ReactNode,title:string}){return <a href={href} className="card p-5 flex items-center gap-3 hover:shadow-md"><span className="text-[#0f4c81]">{icon}</span><span className="font-bold">{title}</span></a>}
