'use client';
import {useEffect,useState} from 'react';
const money=(n:number)=>new Intl.NumberFormat('th-TH',{minimumFractionDigits:2}).format(n)+' บาท';
export default function Reports(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/members').then(r=>r.json()).then(setRows)},[]);const total=rows.reduce((a,r)=>a+Number(r.balance),0);return <div className="min-h-screen p-5 md:p-8"><div className="max-w-7xl mx-auto"><a href="/" className="text-[#0f4c81]">← Dashboard</a><div className="flex justify-between items-end mt-3 mb-6"><div><h1 className="text-2xl font-bold">📈 รายงานเงินออม</h1><p className="text-gray-500">สรุปยอดเงินคงเหลือรายสมาชิก</p></div><button className="btn btn-primary" onClick={()=>window.print()}>พิมพ์รายงาน</button></div><div className="card p-5 mb-5"><div className="text-gray-500">ยอดเงินออมรวม</div><div className="text-3xl font-bold mt-2">{money(total)}</div></div><div className="card p-5 overflow-auto"><table className="table"><thead><tr><th>เลขสมาชิก</th><th>ชื่อ–สกุล</th><th>วันที่สมัคร</th><th>ยอดเงินออม</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.member_no}</td><td>{r.first_name} {r.last_name}</td><td>{new Date(r.joined_at).toLocaleDateString('th-TH')}</td><td>{money(Number(r.balance))}</td></tr>)}</tbody></table></div></div></div>}
