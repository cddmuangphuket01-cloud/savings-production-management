'use client';
import {useState} from 'react';
export default function Setup(){const [f,setF]=useState({username:'admin',full_name:'ผู้ดูแลระบบ',password:'',confirm:''}),[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();if(f.password!==f.confirm)return setMsg('รหัสผ่านไม่ตรงกัน');const r=await fetch('/api/setup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const d=await r.json();if(!r.ok)return setMsg(d.error);location.href='/login'}return <main className="min-h-screen grid place-items-center p-5"><form onSubmit={go} className="card w-full max-w-md p-7"><h1 className="text-2xl font-bold">ตั้งค่าผู้ดูแลระบบ</h1><p className="text-gray-500 mt-1 mb-6">ใช้ครั้งแรกเท่านั้น</p>{msg&&<div className="bg-red-50 text-red-700 p-3 rounded-lg mb-4">{msg}</div>}<label className="text-sm">ชื่อผู้ใช้</label><input className="input mt-1 mb-3" value={f.username} onChange={e=>setF({...f,username:e.target.value})} required/><label className="text-sm">ชื่อ–สกุล</label><input className="input mt-1 mb-3" value={f.full_name} onChange={e=>setF({...f,full_name:e.target.value})} required/><label className="text-sm">รหัสผ่าน</label><input type="password" minLength={8} className="input mt-1 mb-3" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/><label className="text-sm">ยืนยันรหัสผ่าน</label><input type="password" minLength={8} className="input mt-1 mb-5" value={f.confirm} onChange={e=>setF({...f,confirm:e.target.value})} required/><button className="btn btn-primary w-full">สร้างผู้ดูแลระบบ</button><a href="/login" className="block text-center mt-4 text-sm">กลับเข้าสู่ระบบ</a></form></main>}
